<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sme.be_sme.modules.billing.infrastructure.mapper.OnboardingUsageMapper">
  <select id="countOnboardingInstancesByCompanyAndDateRange" resultType="int">
    select count(*)
    from onboarding_instances
    where company_id = #{companyId,jdbcType=VARCHAR}
      and created_at &gt;= #{monthStart,jdbcType=TIMESTAMP}
      and created_at &lt; #{monthEnd,jdbcType=TIMESTAMP}
  </select>
</mapper>
