<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sme.be_sme.modules.survey.infrastructure.mapper.SurveyTemplateMapperExt">

    <select id="selectByIdAndCompanyId"
            resultType="com.sme.be_sme.modules.survey.infrastructure.persistence.entity.SurveyTemplateEntity">
        SELECT
        survey_template_id,
        company_id,
        name,
        stage,
        manager_only,
        status,
        created_by,
        created_at,
        updated_at,
        description,
        version,
        is_default,
        target_role
        FROM survey_templates
        WHERE survey_template_id = #{surveyTemplateId}
        AND company_id = #{companyId}
        LIMIT 1
    </select>

    <select id="existsAnyInstanceByTemplateId" resultType="boolean">
        SELECT CASE WHEN COUNT(1) > 0 THEN TRUE ELSE FALSE END
        FROM survey_instances
        WHERE survey_template_id = #{surveyTemplateId}
        AND company_id = #{companyId}
    </select>

    <select id="existsSentInstanceByTemplateId" resultType="boolean">
        SELECT CASE WHEN COUNT(1) > 0 THEN TRUE ELSE FALSE END
        FROM survey_instances
        WHERE survey_template_id = #{surveyTemplateId}
        AND company_id = #{companyId}
        AND status IN ('SENT', 'COMPLETED', 'EXPIRED')
    </select>

    <delete id="deleteByIdAndCompanyId">
        DELETE FROM survey_templates
        WHERE survey_template_id = #{surveyTemplateId}
        AND company_id = #{companyId}
    </delete>

</mapper>