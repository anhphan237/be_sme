<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sme.be_sme.modules.identity.infrastructure.persistence.mapper.RoleMapperExt">

    <select id="selectRoleIdByCompanyIdAndCode" resultType="string">
        SELECT role_id
        FROM roles
        WHERE company_id = #{companyId}
          AND code = #{code}
          AND status = 'ACTIVE'
            LIMIT 1
    </select>

    <select id="selectRoleIdByCode" resultType="string">
        select role_id
        from roles
        where company_id = #{companyId}
          and code = #{code}
            limit 1
    </select>

</mapper>
