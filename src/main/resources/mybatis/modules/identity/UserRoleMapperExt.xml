<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sme.be_sme.modules.identity.infrastructure.mapper.UserRoleMapperExt">

    <select id="countByCompanyIdAndUserIdAndRoleCode" resultType="int">
        select count(1)
        from user_roles
        where company_id = #{companyId}
          and user_id = #{userId}
          and role_code = #{roleCode}
    </select>

    <insert id="insert">
        insert into user_roles (
            user_role_id, company_id, user_id, role_id, created_at, updated_at
        ) values (
                     #{userRoleId}, #{companyId}, #{userId}, #{roleId}, #{createdAt}, #{updatedAt}
                 )
    </insert>

    <select id="countByCompanyIdAndUserIdAndRoleId" resultType="int">
        SELECT COUNT(1)
        FROM user_roles
        WHERE company_id = #{companyId}
          AND user_id = #{userId}
          AND role_id = #{roleId}
    </select>

    <insert id="insertUserRole">
        INSERT INTO user_roles (user_role_id, company_id, user_id, role_id, created_at)
        VALUES (#{userRoleId}, #{companyId}, #{userId}, #{roleId}, #{createdAt})
    </insert>

    <update id="updateRoleForUser">
        UPDATE user_roles
        SET role_id = #{newRoleId},
            updated_at = #{updatedAt}
        WHERE company_id = #{companyId}
          AND user_id = #{userId}
          AND role_id = #{oldRoleId}
    </update>
</mapper>
